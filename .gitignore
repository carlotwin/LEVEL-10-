# Dependencies
node_modules/

# Runtime data — NEVER commit. Holds uploaded PII (real homeowner
# names/phones/addresses), job state, ledger, logs.
# NOTE: anchored with a leading slash so ONLY the root runtime dir is ignored,
# not the source folder server/data/.
/data/
/uploads/
*.log

# Real source-of-truth spreadsheets (PII). Sandbox seed uses synthetic data.
*Level_10_Properties_with_Contacts*
*.xlsx
*.xls
# Allow synthetic sandbox seed spreadsheets to be committed if desired
!config/sandbox/*.xlsx

# OS / editor
.DS_Store
*.swp
.env
.env.local
