{
  "_README": "REI BlackBook selectors. Most are CONFIRMED/VERIFY from the account's own automation (build 58f8bd4). This app's native send flow = open contact -> Chat tab -> type (TinyMCE iframe) -> Send Text; it does NOT natively do a separate Opt-In or ProfitDial from-number pick, so those keys are blank until captured via `npx playwright codegen`. The engine fails closed: if REQUIRE_OPTIN / REQUIRE_PROFITDIAL are on and their selectors are missing, the contact goes to Needs Review (never sent without them).",
  "login": {
    "emailInput": "input[type='email'], input[name='email'], input[name='username'], input[type='text']",
    "passwordInput": "input[type='password'], input[name='password']",
    "submitButton": "button[type='submit'], button:has-text('Log In'), button:has-text('Login'), button:has-text('Sign In'), input[type='submit']",
    "loggedInMarker": "text=Contacts"
  },
  "contacts": {
    "navContacts": "a:has-text('Smart Contacts'), a[href*='smart-contact' i], a[href*='contact' i], nav a:has-text('Contacts'), aside a:has-text('Contacts'), [role='navigation'] a:has-text('Contacts'), a:has-text('Contacts')",
    "pageMarker": "input[placeholder*='Search By Name' i], input[placeholder*='Name, Phone' i]",
    "tagFilterOpen": "button:has-text('Tags'), text=Tags",
    "tagSearchInput": "input[placeholder='Search...']",
    "tagFilterOption": "text=%TAG%",
    "tagApply": "button:has-text('Apply')",
    "searchInput": "input[placeholder*='Search By Name' i], input[placeholder*='Search by name' i], input[placeholder*='Name, Phone' i], input[placeholder*='Search' i], input[type='search'], input[aria-label*='search' i], input[name*='search' i], .search input",
    "resultRow": "table tbody tr, .list-row, [role='row']",
    "resultRowByText": "text=%QUERY%",
    "openContact": "table tbody tr td a, .list-row a, [role='row'] a",
    "noResults": "text=No Result Found, text=No results found, text=No Results Found",
    "resultName": "",
    "resultPhone": "",
    "resultAddress": ""
  },
  "contact": {
    "nameField": "h1, h2",
    "addressField": "",
    "stateField": "",
    "phoneRows": "a[href^='tel:'], p.chakra-text",
    "tagChips": "[class*='tagTheme' i], span.chakra-badge[class*='tagTheme' i]",
    "notesField": "",
    "chatMessages": ".activity-item, [class*='activity']",
    "pageMarker": "",
    "optInStatus": "[role='dialog'] :text('Opt - In'), [role='dialog'] [role='combobox']",
    "optInButton": "",
    "optInConfirm": "",
    "optInSuccessMarker": "",
    "chatTab": "[role='tab']:has-text('Chat'), text=Chat",
    "editButton": "button[aria-label*='edit' i], [data-testid*='edit' i], button:has(svg[class*='pencil' i]), button:has(svg[class*='edit' i]), [class*='avatar' i] ~ button, [class*='chakra-avatar' i] + button",
    "editModal": "[role='dialog']:has-text('Edit Contact Information'), [role='dialog']:has-text('Contact Information')",
    "optInControl": "[role='dialog'] [role='combobox'], [role='dialog'] button:has-text('Opt'), [role='dialog'] [class*='select' i]:has-text('Opt')",
    "optInOptionIn": "[role='option']:has-text('Opt'), li:has-text('Opt - In'), [role='menuitem']:has-text('Opt')",
    "optInSave": "[role='dialog'] button:has-text('Update Info'), [role='dialog'] button:has-text('Update')"
  },
  "sms": {
    "optInButton": "",
    "optInConfirm": "",
    "optInSuccessMarker": ""
  },
  "chat": {
    "chatTab": "[role='tab']:has-text('Chat'), text=Chat",
    "editorFrame": "iframe.tox-edit-area__iframe, iframe[id*='tiny' i], iframe[title*='Rich Text' i]",
    "profitDialSelect": "button:has-text('From:'), [class*='from' i]:has-text('('), [class*='compose' i] button:has-text('(')",
    "profitDialOptions": "[role='option'], [role='menuitem'], li, [class*='option' i]",
    "profitDialSelectedValue": "text=/From:\\s*.*\\(\\d{3}\\)/, [class*='from' i]",
    "messageInput": "body#tinymce[contenteditable='true'], .mce-content-body[contenteditable='true'], [data-mce-placeholder*='Write Your Reply' i], [aria-placeholder*='Write Your Reply' i], textarea[placeholder*='Write Your Reply' i], [contenteditable='true'], div[role='textbox']",
    "sendButton": "[aria-label='Send Text' i], [title='Send Text' i], button:has-text('Send Text'), [data-testid*='send' i], [aria-label*='send' i], button[title*='send' i], button:has(svg[class*='send' i]), button:has(svg[class*='paper' i])",
    "threadArea": ".timeline, .activity-feed, .activity-history, .chat-thread, [class*='conversation'], [class*='activity']",
    "deliveryStatus": ":text('PD #:'), text=Undelivered",
    "lastInboundMessage": ".activity-item:first-child, [class*='activity']:first-child, .chat-message:last-child",
    "messageThread": ".timeline, .activity-feed, .activity-history, .chat-thread, [class*='conversation'], [class*='activity']",
    "outgoingMessage": ":text('PD #:'), [class*='message' i]:has-text('PD #:')",
    "undeliveredMarker": "text=Undelivered, [class*='undelivered' i], :text('Undelivered')"
  },
  "_verifiedLive": [
    "contacts.searchInput",
    "contacts.resultRow",
    "contacts.openContact"
  ],
  "_verifiedNote": "Keys in _verifiedLive were observed WORKING in the real REI BlackBook account. contacts.searchInput / contacts.resultRow / contacts.openContact: verified by the read-only runs (5/5 phone searches parsed; 3/3 eligible records opened at /contacts/<id>). contact.phoneRows and contact.tagChips are written from the live DOM probe but the selectors themselves have not yet been re-run. A sandbox test never qualifies.",
  "urls": {
    "contactsList": "/contacts",
    "contactDetail": "/contacts/%ID%",
    "contactTab": "/contacts/%ID%?activeTab=%TAB%",
    "manageNumbers": "/profitdial/settings/manage-numbers"
  }
}
